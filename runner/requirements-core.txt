# Minimal install — enough to run `python -m runner embed-test`
# Install the full requirements.txt when you are ready to implement preprocessing.
typer[all]>=0.12
rich>=13.7
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.7
